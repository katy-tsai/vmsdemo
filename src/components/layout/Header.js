import React from 'react';
import Searchbar from './Searchbar';
const Header = (props) => {
    return (
        <div className="header">
            <Searchbar />
        </div>
    );
};

export default Header;